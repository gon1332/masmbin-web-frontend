<%= form_for(@code) do |f| %>
  <% if @code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code.errors.count, "error") %> prohibited this code from being saved:</h2>

      <ul>
      <% @code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :assembly_source %><br>
    <%= f.label :assembly_source %><br>
    <%= f.text_area :assembly_source %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
